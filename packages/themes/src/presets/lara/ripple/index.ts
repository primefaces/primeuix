import type { RippleDesignTokens, RippleTokenSections } from '@primeuix/themes/types/ripple';

export const root: RippleTokenSections.Root = {
    background: 'light-dark(rgba(0,0,0,0.1), rgba(255,255,255,0.3))'
};

export default {
    root
} satisfies RippleDesignTokens;
